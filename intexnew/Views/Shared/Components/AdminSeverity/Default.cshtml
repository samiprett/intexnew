@{
}
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model IEnumerable<int>

<div class="btn-block d-grid gap-2">
    @*<a asp-controller="Home" asp-action="CrashCardsIndex" asp-route-pageNum="1"
        class="btn @(ViewBag.Selected == t ? "btn-primary" : "btn-outline-secondary") btn-block" >Home</a>*@

    @foreach (int t in Model)
    {
        <a asp-controller="Home" asp-action="CrashList" asp-route-severity="@t" asp-route-pageNum="1"
           class="btn @(ViewBag.Selected == t ? "btn-primary" : "btn-outline-secondary") btn-block">@t</a>
    }
</div>
